import * as THREE from "three"
// Texture Loader
const cubeTextureLoader = new THREE.CubeTextureLoader();
const textureLoader = new THREE.TextureLoader();

export {
    cubeTextureLoader, textureLoader
}